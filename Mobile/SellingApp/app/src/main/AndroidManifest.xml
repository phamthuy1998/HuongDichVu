<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="thuypham.n16dccn159.ptithcm.sellingapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".feature.order.OrderDetailActivity"></activity>
        <activity
            android:name=".feature.order.OrderActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".feature.cart.CartActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".feature.authentication.EditProfileActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".feature.authentication.LoginActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".feature.product.ProductsOfCategoryActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".feature.product.ProductDetailActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".feature.splash.SplashActivity"
            android:launchMode="singleTop"
            android:theme="@style/AppTheme.SplashScreen"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".feature.main.MainActivity" />
    </application>

</manifest>